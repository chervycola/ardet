# ДИСКРЕТ · SPA-роутинг: несуществующие пути отдаём приложению,
# чтобы работали ссылки вида дискрет.рф/ЮНОСТЬ-4 (QR у стендов).
RewriteEngine On
RewriteBase /
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . index.html [L]
