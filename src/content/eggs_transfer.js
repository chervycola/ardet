// ═══════════════════════════════════════
// EGGS TRANSFER — перенос пасхального фонда (лист src/assets/EASTER_FUND.md)
// в носители старой игры. Данные генерируются оркестрацией
// (отбор → критика → письмо → приёмка) и заливаются сюда целиком.
// Правило фонда №1: пасхалка — не кнопка; ничего из этого не подсвечивается.
// ═══════════════════════════════════════

// Записки, подбираемые с земли: { x, y, id, text }
export const EGG_LORE = [];

// Уличные таблички: { segment, x, name, form?, facade, backyard?, whisper? }
export const EGG_SIGNS = [];

// Терминальные досье: { 'key': { color, text } }
export const EGG_TERM = {};

// Настенные строки у локаций: { locId: [строки...] }
export const EGG_INSCRIPTIONS = {};

// Узлы карты мира: { id, name, x, y, st, thr }
export const EGG_NODES = [];
